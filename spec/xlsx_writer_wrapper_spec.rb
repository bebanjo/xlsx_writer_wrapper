require 'spec_helper'

describe XlsxWriterWrapper do
  it 'has a version number' do
    expect(XlsxWriterWrapper::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
